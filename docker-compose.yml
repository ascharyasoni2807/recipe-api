version: '3.9'

services:
  web:
    build: .
    command: sh -c "python manage.py migrate && python manage.py create_superuser_with_password || true && gunicorn config.wsgi:application --bind 0.0.0.0:8000"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings
      - DB_NAME=postgres
      - DB_USERNAME=postgres
      - DB_PASSWORD=assignment
      - DB_HOSTNAME=db
      - DB_PORT=5432

  db:
    image: postgres:13
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: assignment

volumes:
  postgres_data:
